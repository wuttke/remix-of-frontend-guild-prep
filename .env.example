# Pocket Dev Guild Frontend Configuration

# Toggle between mock data and real backend
# - "true" (default): Uses mock data, no backend needed
# - "false": Uses real backend at /api (proxied to localhost:8000 in dev)
VITE_USE_MOCK_DATA=true
